// function sumar(parametros) {instruccion}
// # Es igual a escribir #
// sumar = (parametros) => {}
// # Se puede hacer aun mas corto el codigo eliminando las llaves y el return si lo hay
// sumar = (parametros) => instruccion
// # Retorna el resultado de la instruccion

const sumar = (a, b) => a + b;

const result = sumar(8, 9);

document.write(result);
